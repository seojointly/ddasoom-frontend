// 관리자 대시보드(placeholder, ADMIN 전용) — features/admin(프론트 총괄)이 구현.
// README 라우트 표의 /admin, /admin/** (신청·유저·게시글·신고 관리) 자리를 대표한다.
export function AdminDashboardPage() {
  return <p className="p-8 text-center">관리자 대시보드 — 준비 중입니다.</p>;
}
